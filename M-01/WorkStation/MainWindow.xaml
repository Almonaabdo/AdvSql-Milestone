<Window x:Class="WorkStation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Workstation" Height="520" Width="760" WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="2" Text="Workstation — Single Station Simulator"
               FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock VerticalAlignment="Center" Text="Station ID:"/>
            <TextBox x:Name="StationIdBox" IsReadOnly="True" Width="80"  Margin="8,0,0,0" />
            <TextBlock VerticalAlignment="Center" Text="Skill:" Margin="16,0,0,0"/>
            <ComboBox x:Name="SkillBox" Width="150" Margin="8,0,0,0">
                <ComboBoxItem Content="Rookie" IsSelected="True"/>
                <ComboBoxItem Content="Experienced"/>
                <ComboBoxItem Content="Super"/>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="Start" Click="StartBtn_Click" Content="Start" Width="100"  Margin="0,0,8,0"/>
            <Button x:Name="StopBtn" Content="Stop" Width="100" IsEnabled="False"/>
        </StackPanel>

        <DockPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="0,8,0,8">
            <TextBlock Text="State:" FontWeight="SemiBold"/>
            <TextBlock x:Name="StateText" Text="Idle" Margin="6,0,0,0"/>
            <TextBlock Text="   •   TimeScale:" FontWeight="SemiBold" Margin="12,0,0,0"/>
            <TextBlock x:Name="TimeScaleText" Text="(not loaded)"/>
        </DockPanel>

        <GroupBox Grid.Row="3" Grid.ColumnSpan="2" Header="Event Log" Padding="8">
            <ListBox x:Name="LogList"/>
        </GroupBox>


    </Grid>
</Window>
