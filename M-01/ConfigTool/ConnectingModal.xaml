<Window x:Class="ConfigTool.ConnectingModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Margins"
        MinHeight="220"
        MinWidth="320"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" 
        FocusManager.FocusedElement="{Binding ElementName=leftMarginTextBox}">

    <Window.Resources>
        <!-- Secondary button style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="LightGray"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="FontWeight" Value="Normal"/>
        </Style>

        <!-- Primary button style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="10">
        
        <Grid.Resources>
            <!-- Default settings for controls -->
            <Style TargetType="{x:Type Label}">
                <Setter Property="Margin" Value="0,3,5,5" />
                <Setter Property="Padding" Value="0,0,0,5" />
            </Style>
            <Style TargetType="{x:Type TextBox}">
                <Setter Property="Margin" Value="0,0,0,5" />
            </Style>
            <Style TargetType="{x:Type Button}">
                <Setter Property="Width" Value="70" />
                <Setter Property="Height" Value="25" />
                <Setter Property="Margin" Value="5,0,0,0" />
            </Style>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!-- Left,Top,Right,Bottom margins-->
        <Label Grid.Column="0" Grid.Row="0">Server IP Address:</Label>
        <TextBox Name="SourceServerTextBox" Grid.Column="1" Grid.Row="0" />
        
        <Label Grid.Column="0" Grid.Row="1">Database:</Label>
        <TextBox Name="SourceDatabaseTextBox" Grid.Column="1" Grid.Row="1" />

        <Label Grid.Column="0" Grid.Row="2">Login:</Label>
        <TextBox Name="SourceLoginTextBox" Grid.Column="1" Grid.Row="2"/>

        <Label Grid.Column="0" Grid.Row="3">Password:</Label>
        <PasswordBox Height="22" Name="SourcePasswordTextBox" Grid.Column="1" Grid.Row="3" />

        <Label Grid.Column="0" Grid.Row="4">Trust Server Certificate:</Label>
        <CheckBox Name="trustCertificateCheckbox" Margin="0,5,5,5" Grid.Column="1" Grid.Row="4" />

        <!-- Buttons -->
        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource SecondaryButtonStyle}" Click="CloseButton_Click">Close</Button>
            <Button Style="{StaticResource PrimaryButtonStyle}"   Click="ConnectButton_Click" IsDefault="True">Connect</Button>
        </StackPanel>
    </Grid >
</Window>